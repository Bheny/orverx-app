import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Orvex — Where Fashion Creatives Connect",
  description:
    "Orvex is the collaboration platform for fashion students. Create projects, build your portfolio, and connect with fellow creatives.",
  openGraph: {
    title: "Orvex — Where Fashion Creatives Connect",
    description:
      "Create projects, build your portfolio, and connect with fellow fashion students.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
